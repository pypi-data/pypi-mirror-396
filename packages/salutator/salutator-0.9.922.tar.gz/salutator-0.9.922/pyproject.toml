[build-system]
requires = ["setuptools>=42","wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "salutator/_version.py"
version_scheme = "post-release"
local_scheme = "node-and-date"

