[project]
name = 'ovrtx'
authors = [
    {name = 'NVIDIA Kitmaker Team', email = 'kitmaker@nvidia.com' },
]
description = 'Zero version placeholder for ovrtx'
license = {text = 'Apache-2.0'}
dependencies = []
version = '0.0.0a0'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'
