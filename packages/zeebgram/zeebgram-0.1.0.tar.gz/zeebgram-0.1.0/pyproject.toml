[project]
name = "zeebgram"
version = "0.1.0"
description = "Deskripsi project zeebgram"
readme = "README.md"
requires-python = ">=3.7"
license = {file = "LICENSE"}

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
