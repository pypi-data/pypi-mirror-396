[project]
name = "zeebgram"
version = "0.1.1"
description = "Custom fork of Pyrogram for userbot"
readme = "README.md"
requires-python = ">=3.7"
license = {file = "LICENSE"}

dependencies = [
    "tgcrypto"
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
