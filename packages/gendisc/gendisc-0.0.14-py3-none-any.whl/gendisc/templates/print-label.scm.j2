(define (print-label filename)
  (let* (
        (image (car (gimp-file-load RUN-INTERACTIVE filename filename)))
        )
    (file-print-gtk #:run-mode RUN-INTERACTIVE #:image image)
  )
)
(print-label "{{ label_file }}")
