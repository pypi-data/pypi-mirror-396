- name: {{ template.bounded_context }}
  type: directory
  python: True
  children:
    - name: {{ template.aggregate_name }}
      type: directory
      python: True
      children:
        - name: application
          type: directory
          python: True
        - name: domain
          type: directory
          python: True
        - name: infrastructure
          type: directory
          python: True