[project]
name = "omdb-microwave"
version = "0.1.0"
description = ""
authors = [
    {name = "miguel bedard",email = "miguelbedard36@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "omdb_microwave", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
