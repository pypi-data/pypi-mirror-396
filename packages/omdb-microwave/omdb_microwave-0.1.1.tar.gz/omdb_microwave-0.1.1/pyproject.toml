[project]
name = "omdb-microwave"
version = "0.1.1"
description = ""
authors = [
    {name = "miguel bedard",email = "miguelbedard36@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "omdb_microwave", from = "src"}]

[tool.poetry.scripts]
commandname = 'omdb_microwave.cli:main'

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
