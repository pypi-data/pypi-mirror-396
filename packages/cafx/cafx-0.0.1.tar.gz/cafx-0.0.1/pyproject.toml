[project]
name = "cafx"
version = "0.0.1"
description = "Placeholder for the upcoming CAF."
authors = [{ name = "Opeyemi Bamigbade" }]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
