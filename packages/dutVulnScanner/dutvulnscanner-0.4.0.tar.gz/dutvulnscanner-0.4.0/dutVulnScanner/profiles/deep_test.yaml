name: deep_test
description: Deep vulnerability testing - DEMO MODE (dangerous tools disabled)

metadata:
  estimated_duration: "~5 minutes"
  risk_level: "Safe (Demo Mode)"

tools:
  - whatweb
  - nmap

tool_configs:
  whatweb:
    aggression: 1
    timeout: 10
  
  nmap:
    args:
      - "-sV"
      - "-F"
      - "-T4"

parallel: true
timeout: 300   # 5 minutes for demo
enable_correlation: true

# WARNING: This profile performs active exploitation attempts
# Only use with explicit written authorization!
warning: |
  ⚠️  AUTHORIZATION REQUIRED ⚠️
  This profile performs active vulnerability exploitation and brute-force attacks.
  Ensure you have explicit written permission before running this scan.
  Unauthorized use may be illegal and unethical.
