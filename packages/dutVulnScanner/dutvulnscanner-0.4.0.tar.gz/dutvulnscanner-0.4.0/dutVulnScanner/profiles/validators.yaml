name: validators
description: Vulnerability validation and verification testing

metadata:
  estimated_duration: "~10 minutes"
  risk_level: "Safe"

tools:
  - sslscan

tool_configs:
  sslscan:
    show_certificate: false
    check_vulnerabilities: true

parallel: true
timeout: 600  # 10 minutes for demo
enable_correlation: true
