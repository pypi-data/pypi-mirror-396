[project]
name = "maxentsolver"
version = "0.2.5"
description = "A Python package for fitting maximum entropy models to BOLD time-series data. The toolkit automatically binarizes fMRI/BOLD signals, constructs pairwise maximum entropy (MaxEnt) models, and provides utilities to analyze the inferred interaction network. Most functionality is not implemented yet."
authors = [
    {name = "Marc Vöhringer Carrera"}
]
license = {text = "MIT License"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch (>=2.0.0,<3.0.0)",
    "numpy (>=1.0.0,<3.0.0)",
    "matplotlib (>=1.0.0,<4.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
