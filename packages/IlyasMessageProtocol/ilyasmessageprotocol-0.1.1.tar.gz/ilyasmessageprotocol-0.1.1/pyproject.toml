[project]
name = "IlyasMessageProtocol"
version = "0.1.1"
description = "Моя библиотека imp_utils"
readme = "README.md"
authors = [{ name = "Qwamatata", email = "malyutin.2011@list.ru" }]
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
