[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eml-cli"
version = "0.0.0"
description = "Migrate emails between IMAP mailboxes with flexible filtering"
requires-python = ">=3.10"