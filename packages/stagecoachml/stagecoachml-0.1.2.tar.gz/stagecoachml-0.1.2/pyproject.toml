[build-system]
requires = ["uv_build>=0.9.11,<0.10.0"]
build-backend = "uv_build"

[project]
name = "stagecoachml"
version = "0.1.2"
description = "A library for two-stage machine learning models with staggered feature arrival"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "Gaurav Sood", email = "contact@gsood.com"}
]
maintainers = [
    {name = "Gaurav Sood", email = "contact@gsood.com"}
]
keywords = [
    "machine learning",
    "two-stage models",
    "staged features",
    "early prediction",
    "residual learning",
    "scikit-learn",
    "multi-stage inference",
    "feature arrival",
    "latency optimization",
    "progressive ml"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed"
]
dependencies = [
    "numpy>=1.20.0",
    "pandas>=1.3.0", 
    "scikit-learn>=1.0.0",
    "psutil>=5.8.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "pytest-xdist>=3.5.0",
    "pytest-mock>=3.12.0",
    "hypothesis>=6.90.0",
    "ruff>=0.1.0",
    "mypy>=1.7.0",
    "pre-commit>=3.5.0",
    "types-pyyaml>=6.0.0",
    "pandas-stubs>=2.0.0"
]
docs = [
    "sphinx>=7.2.0",
    "furo>=2023.9.10",
    "sphinx-autodoc-typehints>=1.25.0",
    "myst-parser>=2.0.0",
    "sphinx-copybutton>=0.5.2",
    "sphinx-design>=0.5.0",
    "nbsphinx>=0.9.3"
]
all = [
    "stagecoachml[dev,docs]"
]

[project.urls]
Homepage = "https://github.com/finite-sample/stagecoachml"
Documentation = "https://finite-sample.github.io/stagecoachml/"
Repository = "https://github.com/finite-sample/stagecoachml"
"Bug Tracker" = "https://github.com/finite-sample/stagecoachml/issues"
Changelog = "https://github.com/finite-sample/stagecoachml/blob/main/CHANGELOG.md"


[tool.uv.build-backend]
module-root = ""

[tool.deptry]
pep621_dev_dependency_groups = ["dev", "docs"]

[tool.deptry.package_module_name_map]
"types-pyyaml" = "types_pyyaml"


[tool.ruff]
target-version = "py311"
line-length = 100
src = ["stagecoachml", "tests"]
extend-include = ["*.ipynb"]
extend-exclude = ["examples/**"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "UP",   # pyupgrade
    "ARG",  # flake8-unused-arguments
    "SIM",  # flake8-simplify
    "PTH",  # flake8-use-pathlib
    "RUF",  # ruff-specific rules
    "N",    # pep8-naming
    "D",    # pydocstyle
    "TID",  # flake8-tidy-imports
    "TCH",  # flake8-type-checking
    "PL",   # pylint
    "PT",   # flake8-pytest-style
]
ignore = [
    "D100", # Missing docstring in public module
    "D101", # Missing docstring in public class
    "D102", # Missing docstring in public method
    "D103", # Missing docstring in public function
    "D104", # Missing docstring in public package
    "D105", # Missing docstring in magic method
    "D107", # Missing docstring in __init__
    "D203", # 1 blank line required before class docstring
    "D213", # Multi-line docstring summary should start at the second line
    "PLR0913", # Too many arguments
    "PLR2004", # Magic value used in comparison
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "D104"]
"tests/*" = ["D", "S101", "ARG001", "ARG002", "ARG005", "N806", "PLC0415", "PT006", "SIM113", "UP035"]
"docs/*" = ["D", "PTH100", "PTH110"]
"stagecoachml/cli.py" = ["B008", "ARG001", "PTH123", "B904"]
"stagecoachml/stage.py" = ["PLC0415", "N806", "ARG002", "D106", "D401", "TC003"]
"stagecoachml/pipeline.py" = ["TC001", "D106", "B904"]

[tool.ruff.lint.isort]
known-first-party = ["stagecoachml"]

[tool.ruff.format]
docstring-code-format = true

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
no_implicit_reexport = true
namespace_packages = true
show_error_codes = true
show_column_numbers = true
pretty = true

[[tool.mypy.overrides]]
module = [
    "sklearn.*",
    "joblib.*",
    "networkx.*",
    "yaml.*"
]
ignore_missing_imports = true

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers"
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
    "benchmark: marks tests as benchmarks"
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
    "ignore::UserWarning"
]

[tool.coverage.run]
branch = true
source = ["stagecoachml"]
omit = [
    "*/tests/*",
    "*/__init__.py",
    "*/cli.py"
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
    "@overload"
]
precision = 2
show_missing = true