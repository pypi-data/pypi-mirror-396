[project]
name = "model-track-cr"
version = "0.2.2"
description = ""
authors = [
    { name = "Cristiano", email = "cristiano2132.ufv@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.10,<4.0"

dependencies = [
    "numpy (>=1.23,<2.0)",
    "pandas (>=1.5,<3.0)",
    "scikit-learn (>=1.2,<2.0)",
    "matplotlib (>=3.7,<4.0)",
    "seaborn (>=0.12,<0.14)"
]

[tool.poetry]
packages = [{ include = "model_track", from = "src" }]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=7.4,<9.0)",
    "pytest-cov (>=4.1,<6.0)"
]