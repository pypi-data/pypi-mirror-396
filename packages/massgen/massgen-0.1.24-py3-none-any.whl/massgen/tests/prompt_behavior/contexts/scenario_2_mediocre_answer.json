{
  "name": "Mediocre Incomplete Answer",
  "description": "A basic but incomplete answer. Should trigger new_answer on balanced and strict.",
  "original_message": "Explain how to implement a binary search tree in Python with insert, search, and delete operations.",
  "existing_answers": {
    "agent_a": "Here's a basic binary search tree:\n\n```python\nclass Node:\n    def __init__(self, value):\n        self.value = value\n        self.left = None\n        self.right = None\n\nclass BST:\n    def __init__(self):\n        self.root = None\n    \n    def insert(self, value):\n        if not self.root:\n            self.root = Node(value)\n        else:\n            self._insert_recursive(self.root, value)\n    \n    def _insert_recursive(self, node, value):\n        if value < node.value:\n            if node.left:\n                self._insert_recursive(node.left, value)\n            else:\n                node.left = Node(value)\n        else:\n            if node.right:\n                self._insert_recursive(node.right, value)\n            else:\n                node.right = Node(value)\n```\n\nThis implements basic insert functionality for a BST."
  },
  "expected_behavior": {
    "lenient": "vote (basic implementation provided)",
    "balanced": "new_answer (missing search and delete, no error handling)",
    "strict": "new_answer (incomplete, missing operations)"
  }
}
