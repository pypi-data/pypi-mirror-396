[tool.poetry]
name = "traceAI-google-genai"
version = "0.1.1"
description = "OpenTelemetry instrumentation for Google Gen AI"
authors = ["Future AGI <no-reply@futureagi.com>"]
readme = "README.md"

packages = [
    { include = "traceai_google_genai" },
]

[tool.poetry.dependencies]
python = ">3.9, <3.14"
google-genai = ">=1.8.0"
fi-instrumentation-otel = ">=0.1.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
