[project]
name = "ngit"
version = "0.1.0"
description = "Custom Git wrapper CLI"
authors = [{name="Your Name"}]
dependencies = ["typer", "gitpython"]

[project.scripts]
ngit = "ngit.ngit:app"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
