{% if link_schema -%}
{{ link_source_name }} > fc.Link({{ link_name }}).use_schema({{ link_schema }}) > {{ link_target_name }}
{%- else -%}
{{ link_source_name }} > fc.Link({{ link_name }}) > {{ link_target_name }}
{%- endif %}
