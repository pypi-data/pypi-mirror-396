{% if styled -%}
{{ var_name }} = (
    {{ composer }}.add_styled_comment({{ content }})
    {% for prop, val in formats.items() -%}
    .set_{{ prop }}({{ val }})
    {%- endfor %}
)
{%- endif %}

{% if markdown -%}
{{ var_name }} = {{ composer }}.add_markdown_comment({{ content }})
{%- endif %}
