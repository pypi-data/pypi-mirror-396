
connection = project.create_connection(
    name="{{ name }}",
    datasource_type=platform.datasources.get(datasource_id="{{ datasource_id }}"),
    description={% if description is none %}None{% else %}"{{ description }}"{% endif %},
    properties={{ properties }}
)
{% raw %}{% endraw %}
