[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "noahs_tts"  # Must be unique on PyPI!
version = "0.1.2"
description = "A wrapper class to get started using Text To Speech quickly"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
  { name = "Noah Jones", email = "jonesnoah45010@gmail.com" }
]
dependencies = [
  "pyttsx3",
]

[project.urls]
"Homepage" = "https://github.com/jonesnoah45010/noahs_tts"
