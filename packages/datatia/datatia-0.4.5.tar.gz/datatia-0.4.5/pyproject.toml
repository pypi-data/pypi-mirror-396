[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "datatia"
version = "0.4.5"
description = "Opinionated declarative utility library for writing PyTorch dataset classes"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "torch"
]
