# Config file for examples/blq.py
env: {}

pipeline:
  segments:
    - steps_config:
        ingest:
          starts_segment: True
          bootstrap_servers:
            - "localhost:9092"

        kafkasink:
          bootstrap_servers:
            - "localhost:9092"

        kafkasink2:
          bootstrap_servers:
            - "localhost:9092"

        kafkasink3:
          bootstrap_servers:
            - "localhost:9092"
