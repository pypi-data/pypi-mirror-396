[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tsim"
version = "0.1.0"
description = "A lightweight library for quantifying the similarity between two strings"
readme = "README.md"
authors = [{ name = "Sanyam Asthana", email = "sanyam.asthana@gmail.com" }]
dependencies = []
