<!DOCTYPE html>
<html>
<head>
    <title>{{ error_title }}</title>
    <link rel="stylesheet" href="{{ base_path | default('') }}/numerous-static/css/numerous-base.css">
    <style>
        body {
            font-family: var(--numerous-font-sans);
            line-height: 1.6;
            max-width: 800px;
            margin: 40px auto;
            padding: 0 20px;
            color: var(--numerous-color-text);
            background: var(--numerous-color-bg);
        }
    </style>
</head>
<body>
    <div class="numerous-error-container">
        <h1>{{ error_title }}</h1>
        <div class="numerous-error-content">{{ error_message }}</div>
    </div>
</body>
</html>
