<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Survey App</title>
    <style>
        body {
            font-family: var(--numerous-font-sans, 'Inter', sans-serif);
            background: var(--numerous-color-bg, #0f0f13);
            color: var(--numerous-color-text, #e4e4e7);
            margin: 0;
            padding: 2rem;
            min-height: 100vh;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        h1 {
            color: var(--numerous-color-primary, #6366f1);
            margin-bottom: 0.5rem;
        }

        .subtitle {
            color: var(--numerous-color-text-muted, #71717a);
            margin-bottom: 2rem;
        }

        .card {
            background: var(--numerous-color-surface, #1a1a22);
            border: 1px solid var(--numerous-color-border, #27272a);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1rem;
        }

        .widget-row {
            display: flex;
            gap: 1rem;
            align-items: center;
            margin-bottom: 1rem;
        }

        nav {
            margin-bottom: 2rem;
        }

        nav a {
            color: var(--numerous-color-primary, #6366f1);
            text-decoration: none;
            margin-right: 1rem;
        }

        nav a:hover {
            text-decoration: underline;
        }

        .badge {
            background: #ef4444;
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.75rem;
            margin-left: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <nav>
            <a href="/">Survey App</a>
            <a href="/management">Management <span class="badge">ADMIN</span></a>
        </nav>

        <h1>📋 Public Survey</h1>
        <p class="subtitle">No authentication required</p>

        <div class="card">
            <h2>Survey Submissions</h2>
            <div class="widget-row">
                {{ survey_counter }}
                {{ submit_btn }}
            </div>
        </div>
    </div>
</body>
</html>

