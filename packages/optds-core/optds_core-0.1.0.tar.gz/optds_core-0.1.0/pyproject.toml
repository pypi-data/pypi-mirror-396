[project]
name = "optds-core"
version = "0.1.0"
description = "Core types and schemas for OptDS"
readme = "README.md"
authors = [
    { name = "Raeid Saqur", email = "raeid.saqur@maths.ox.ac.uk" }
]
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "typing_extensions",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"