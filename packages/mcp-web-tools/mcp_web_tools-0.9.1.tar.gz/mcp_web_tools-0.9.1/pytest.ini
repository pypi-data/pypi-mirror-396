[pytest]
asyncio_mode = auto
addopts = --cov=mcp_web_tools --cov-report=term-missing
markers =
    live_search: marks live search integration tests (set RUN_LIVE_SEARCH_TESTS=1)
    live_loader: marks live loader integration tests (set RUN_LIVE_LOADER_TESTS=1)
