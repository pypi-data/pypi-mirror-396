[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "SATRL"
version = "0.0.0"
description = "Simple RL baseline library"
readme = "README.md"
requires-python = ">=3.8"