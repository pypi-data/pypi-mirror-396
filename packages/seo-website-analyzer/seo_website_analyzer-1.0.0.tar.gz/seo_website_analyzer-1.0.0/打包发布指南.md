# 📦 SEO网站分析工具 - 打包发布指南

本指南说明如何将此工具打包并发布到PyPI。

## 📋 前置要求

1. **Python环境**: Python 3.7+
2. **安装构建工具**:
   ```bash
   pip install build twine
   ```

3. **PyPI账号**: 
   - 测试环境: https://test.pypi.org/account/register/
   - 正式环境: https://pypi.org/account/register/

## 🚀 打包流程

### 1. 准备源代码

确保所有文件都已准备好：
- ✅ `pyproject.toml` - 包配置文件
- ✅ `MANIFEST.in` - 包含文件清单
- ✅ `README.md` - 项目说明
- ✅ 源代码文件

### 2. 清理旧文件

```bash
# 删除旧的构建产物
rm -rf build/
rm -rf dist/
rm -rf *.egg-info/
```

### 3. 构建包

使用 `build` 工具创建源码分发包和wheel：

```bash
# 同时创建sdist和wheel
python -m build

# 或者分别创建
python -m build --sdist    # 仅创建源码分发包
python -m build --wheel    # 仅创建wheel
```

构建完成后，会在 `dist/` 目录下生成：
- `seo-website-analyzer-1.0.0.tar.gz` (源码分发包)
- `seo_website_analyzer-1.0.0-py3-none-any.whl` (wheel包)

### 4. 检查构建产物（可选）

```bash
# 使用twine检查包
twine check dist/*
```

### 5. 上传到PyPI

#### 方法1: 使用twine（推荐）

**上传到测试环境（TestPyPI）**:
```bash
twine upload --repository testpypi dist/*
# 输入 TestPyPI 的用户名和密码/API token
```

**上传到正式环境（PyPI）**:
```bash
twine upload dist/*
# 输入 PyPI 的用户名和密码/API token
```

#### 方法2: 使用API Token（推荐，更安全）

1. 在PyPI网站生成API token
2. 创建 `~/.pypirc` 文件:
   ```ini
   [distutils]
   index-servers =
       pypi
       testpypi

   [pypi]
   username = __token__
   password = pypi-你的API-token

   [testpypi]
   repository = https://test.pypi.org/legacy/
   username = __token__
   password = pypi-你的TestPyPI-API-token
   ```

3. 然后直接上传:
   ```bash
   twine upload dist/*  # 正式环境
   # 或
   twine upload --repository testpypi dist/*  # 测试环境
   ```

### 6. 验证发布

上传成功后，可以通过pip安装测试：

```bash
# 从测试环境安装
pip install -i https://test.pypi.org/simple/ seo-website-analyzer

# 从正式环境安装
pip install seo-website-analyzer
```

## 📝 更新版本

更新版本需要修改 `pyproject.toml` 中的版本号：

```toml
[project]
version = "1.0.1"  # 更新版本号
```

然后重新执行构建和上传流程。

## 🎯 使用发布的包

用户安装后可以通过以下方式使用：

```bash
# 通过命令行使用（因为配置了入口点）
seo-checker https://example.com

# 或作为Python模块使用
python -m seo_checker https://example.com

# 或在Python代码中导入
from seo_checker import SEOChecker
checker = SEOChecker()
checker.check_website("https://example.com")
```

## ⚠️ 注意事项

1. **版本号**: 每次发布必须更新版本号
2. **依赖检查**: 确保 `requirements.txt` 和 `pyproject.toml` 中的依赖一致
3. **测试**: 建议先在TestPyPI上测试
4. **文档**: 确保README.md完整且准确
5. **许可证**: 确保已包含LICENSE文件（如果使用MIT许可证）

## 🔧 故障排除

### 问题1: 构建失败
- 检查 `pyproject.toml` 语法
- 确保所有依赖都已安装

### 问题2: 上传失败
- 检查PyPI账号权限
- 确保版本号唯一（不能重复上传相同版本）
- 检查网络连接

### 问题3: 安装后无法使用命令
- 检查 `[project.scripts]` 配置
- 确保入口点函数名正确

## 📚 参考资源

- [Python Packaging User Guide](https://packaging.python.org/)
- [PyPI Documentation](https://pypi.org/help/)
- [hatchling Documentation](https://hatch.pypa.io/)

