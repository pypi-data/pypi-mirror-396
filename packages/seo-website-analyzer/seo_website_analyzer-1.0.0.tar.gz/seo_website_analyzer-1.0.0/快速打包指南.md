# 🚀 快速打包指南

## 一键打包（Windows）

直接运行批处理脚本：

```bash
build_package.bat
```

## 手动打包步骤

### 1. 安装构建工具

```bash
pip install build twine
```

### 2. 清理旧文件

```bash
# Windows
rmdir /s /q build dist *.egg-info

# Linux/Mac
rm -rf build dist *.egg-info
```

### 3. 构建包

```bash
python -m build
```

### 4. 检查包

```bash
twine check dist/*
```

### 5. 上传到PyPI

**测试环境（推荐先测试）**:
```bash
twine upload --repository testpypi dist/*
```

**正式环境**:
```bash
twine upload dist/*
```

## 📦 测试安装

在正式上传前，可以先本地测试安装：

```bash
pip install dist/seo_website_analyzer-1.0.0-py3-none-any.whl
```

然后测试命令：
```bash
seo-checker https://example.com
```

## ⚠️ 重要提示

1. **版本号**: 每次发布前更新 `pyproject.toml` 中的版本号
2. **测试**: 建议先在 TestPyPI 上测试
3. **API Token**: 推荐使用 PyPI API Token 而非密码
4. **README**: 确保 README.md 完整准确

## 📝 更新版本流程

1. 修改 `pyproject.toml` 中的 `version`
2. 运行 `build_package.bat` 或手动构建
3. 检查并上传

