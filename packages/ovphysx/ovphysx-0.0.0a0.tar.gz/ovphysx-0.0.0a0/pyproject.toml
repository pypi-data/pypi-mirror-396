[project]
name = 'ovphysx'
authors = [
    {name = 'NVIDIA Kitmaker Team', email = 'kitmaker@nvidia.com' },
]
description = 'Zero version placeholder for ovphysx'
license = {text = 'Apache-2.0'}
dependencies = []
version = '0.0.0a0'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'
