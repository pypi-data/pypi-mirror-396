[tox]
envlist = py3

[testenv]
deps=-r{toxinidir}/requirements.txt
     -r{toxinidir}/test-requirements.txt

commands=
    pytest --cov=utf_queue_client

[flake8]
exclude = .venv,.git,.tox,docs,venv,bin,lib,deps,build
ignore =
    E501,
    W503,
    E203,
    D202,
    W504