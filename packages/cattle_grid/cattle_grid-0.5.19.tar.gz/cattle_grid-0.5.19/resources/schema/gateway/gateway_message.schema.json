{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "title": "Gateway Message",
  "description": "Message send to the gateway then forwarded to cattle_grid",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "self",
        "send",
        "fetch",
        "profile",
        "options",
        "manage_actor",
        "delete_actor"
      ],
      "description": "Action to be taken by the gateway"
    },
    "actor": {
      "type": "string",
      "description": "actor_id of the actor that is performing the action. Actor must belong to the user"
    },
    "data": {
      "type": "object",
      "description": "Activity to be send or object to fetch/delete etc ... not sure yet. this seems awkward"
    }
  },
  "required": ["action", "data", "actor"]
}
