services:
  jwt-auth-bypass:
    container_name: jwt-auth-bypass
    image: jedha/lab-jwt-auth-bypass
    expose:
      - 3000
    networks:
      jwt-auth-bypass:
        ipv4_address: 10.10.3.27

networks:
  jwt-auth-bypass:
    driver: bridge
    ipam:
      config:
        - subnet: 10.10.3.0/24
