services:
  csrf:
    container_name: csrf
    image: jedha/lab-csrf-password-change
    expose:
      - 5001
    networks:
      csrf:
        ipv4_address: 10.10.3.28

networks:
  csrf:
    driver: bridge
    ipam:
      config:
        - subnet: 10.10.3.0/24
