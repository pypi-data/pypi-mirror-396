services:
  user_management:
    container_name: sql-ex
    image: jedha/lab-sql-ex
    networks:
      lab-network:
        ipv4_address: 11.10.10.25
    expose:
      - 5432

networks:
  lab-network:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 11.10.10.0/24