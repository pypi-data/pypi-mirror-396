[project]
name = "keygenie"
version = "0.0.1"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyautogui>=0.9.54",
    "pynput>=1.8.0",
]

[project.scripts]
kg = "keygenie.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"