[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mathtypes"
version = "0.1.2"
description = "A simple package that provides you math types like fraction"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name="BK Darsh", email="darshtyagidt15@email.com" }
]
license = { text = "MIT" }
