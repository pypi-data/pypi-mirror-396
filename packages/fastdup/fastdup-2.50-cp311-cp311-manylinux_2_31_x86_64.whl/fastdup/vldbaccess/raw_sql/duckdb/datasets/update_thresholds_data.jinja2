UPDATE datasets
SET thresholds = (
    SELECT list(distinct similarity_threshold ORDER BY similarity_threshold ASC)
    FROM similarity_clusters
    WHERE similarity_clusters.dataset_id = datasets.id
    AND datasets.id = :dataset_id
)