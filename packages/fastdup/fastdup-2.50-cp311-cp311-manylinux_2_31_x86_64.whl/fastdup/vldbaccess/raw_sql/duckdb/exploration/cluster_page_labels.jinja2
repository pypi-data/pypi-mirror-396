WITH
{% include "_similarity_filtering_cte.jinja2" %},
page_of_clusters AS (
    {% include "_page_of_clusters.jinja2" %}
),
ranked_labels AS (
    {% include "_ranked_labels.jinja2" %}
)
SELECT
    cluster_id, LIST(label_count) top_labels
FROM ranked_labels
GROUP BY cluster_id
;
