        SELECT
            image_issues.image_id image_or_object_id,
            array_remove(array_agg(DISTINCT image_issues.type_id), NULL) issue_type_ids
        FROM
            image_issues
        WHERE
            image_issues.cause IS NULL
            AND image_issues.dataset_id = :dataset_id
        GROUP BY
            image_issues.image_id
