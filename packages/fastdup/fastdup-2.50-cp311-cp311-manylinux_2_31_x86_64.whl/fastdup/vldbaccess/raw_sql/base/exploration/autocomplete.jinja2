SELECT caption, labels
FROM flat_similarity_clusters
WHERE dataset_id = :dataset_id
    AND cluster_type='IMAGES'
    AND caption IS NOT NULL
    {% include "_full_text_search_filter.jinja2" %}
LIMIT {{limit}};
