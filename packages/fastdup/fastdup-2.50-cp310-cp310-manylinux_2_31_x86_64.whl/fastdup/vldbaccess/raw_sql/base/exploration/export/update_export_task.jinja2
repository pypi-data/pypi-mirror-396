UPDATE export_task
SET status = :export_task_status
    {% if progress is not none %},progress = :progress {% endif %}
    {% if download_uri is not none %},download_uri = :download_uri {% endif %}
WHERE id = :export_task_id
