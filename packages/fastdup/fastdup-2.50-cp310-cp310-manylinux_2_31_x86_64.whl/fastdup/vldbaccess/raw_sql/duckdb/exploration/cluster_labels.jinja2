WITH
{% include "_similarity_filtering_cte.jinja2" %}
SELECT cluster_id, LIST(array_cat(labels, vl_labels)) as labels
FROM main.flat_similarity_clusters
{% include "_similarity_join_filtering.jinja2" %}
WHERE labels is not null
{% include "_composite_filter.jinja2" %}
GROUP BY cluster_id
