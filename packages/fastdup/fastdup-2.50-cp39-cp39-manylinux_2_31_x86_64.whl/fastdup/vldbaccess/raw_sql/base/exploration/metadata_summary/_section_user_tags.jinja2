(
SELECT
    'user_tags' as category,
    tags.name as element_name,
    count(*) as element_count
FROM cluster_media_ids
    JOIN media_to_tags ON cluster_media_ids.media_id = media_to_tags.media_id
    JOIN tags ON media_to_tags.tag_id = tags.id
GROUP BY category, element_name
)
