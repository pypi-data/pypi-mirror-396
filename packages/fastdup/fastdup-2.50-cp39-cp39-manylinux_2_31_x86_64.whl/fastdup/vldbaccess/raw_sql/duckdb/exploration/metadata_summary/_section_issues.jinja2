SELECT
    'issues' as category,
    name element_name,
    COUNT(*) as element_count
FROM
(
SELECT issue_type.name
FROM (SELECT unnest(issue_type_ids) issue_type_id FROM cluster_media_ids) t0
JOIN issue_type ON t0.issue_type_id = issue_type.id
) t1
GROUP BY category, element_name