(
SELECT
category,
vl_label as element_name,
count(*) element_count
FROM (SELECT 'vl_labels_' || lower(cluster_type::text) as category, unnest(vl_labels) as vl_label FROM cluster_media_ids) t
WHERE vl_label IS NOT NULL
GROUP BY category, element_name
)