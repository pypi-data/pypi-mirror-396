(
SELECT
category,
label as element_name,
count(*) element_count
FROM (SELECT lower(cluster_type::text) as category, unnest(labels) as label FROM cluster_media_ids) t
WHERE label IS NOT NULL
GROUP BY category, element_name
)