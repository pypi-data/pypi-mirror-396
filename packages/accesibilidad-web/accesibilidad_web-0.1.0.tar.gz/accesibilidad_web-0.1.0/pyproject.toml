[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "accesibilidad-web"
version = "0.1.0"
description = "Librería de accesibilidad web con reconocimiento de voz y gestos"
readme = "README.md"
authors = [
  { name = "AKITSUAKANE" }
]
license = { text = "MIT" }
requires-python = ">=3.8"

dependencies = [
  "SpeechRecognition",
  "PyAudio",
  "opencv-python",
  "numpy"
]
