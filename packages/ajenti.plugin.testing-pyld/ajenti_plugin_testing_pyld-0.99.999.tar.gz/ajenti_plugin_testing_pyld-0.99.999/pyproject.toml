[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ajenti.plugin.testing-pyld"
version = "0.99.999"
description = "Ajenti secret extractor plugin"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
