[project]
name = "tinyvec2d"
version = "0.1.0"
description = "Tiny 2D vector helpers, no NumPy, no classes."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "Melvin" }
]
dependencies = []

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"