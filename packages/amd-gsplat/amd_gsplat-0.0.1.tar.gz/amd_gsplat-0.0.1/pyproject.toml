[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "amd-gsplat"
version = "0.0.1"
description = "gsplat is an open-source library for GPU-accelerated rasterization of Gaussians with Python bindings."
authors = [
    { name="AMD", email="support@amd.com" }
]

readme = "README.md"
requires-python = ">=3.8"
license = {text = "Apache 2.0"}
classifiers = [    
]

[project.urls]
"Homepage" = "https://github.com/rocm/gsplat"
