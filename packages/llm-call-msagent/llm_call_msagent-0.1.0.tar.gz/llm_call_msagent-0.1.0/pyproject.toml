[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


[project]
name = "llm-call-msagent"             # package name on pip
version = "0.1.0"
description = "Simple Azure OpenAI call helper"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "python-dotenv",
    "azure-identity",
    "agent-framework",          # or agent-framework-azure if that's correct
]


[project.urls]
homepage = "https://example.com"
