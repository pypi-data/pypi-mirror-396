# Avoid having to prefix python commands with uv run
direnv_load uv run direnv dump

# Default environment settings in version control
# dotenv .env.dev

# Overrides not under version control
dotenv_if_exists .env