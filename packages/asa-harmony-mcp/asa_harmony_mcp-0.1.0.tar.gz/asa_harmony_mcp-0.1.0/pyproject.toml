[project]
name = "asa-harmony-mcp"
version = "0.1.0"
description = "鸿蒙开发文档MCP服务"
readme = "README.md"
authors = [
    { name = "孤单的城", email = "10196842+tianlangaa@user.noreply.gitee.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.24.0",
]

[project.scripts]
asa-harmony-mcp = "asa_harmony_mcp:main"

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"
