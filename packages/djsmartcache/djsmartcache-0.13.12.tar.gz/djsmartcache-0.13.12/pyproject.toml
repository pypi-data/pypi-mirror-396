[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "djsmartcache"
version = "0.13.12"
description = "Distributed cache consistency layer for Django using Redis"
authors = [{name="Kuntal Roy"}]
readme = "README.md"
requires-python = ">=3.8"
license = {text="MIT"}
