name = A requirement manager Agent (LLM based)
doc = An agent that generates a list of requirements from a specification. Uses a LLM.


input : spec : 1 : A specification to manage.
action : build : 0 : generate a list of requirements.
input : sleep_time : 1 : Configure the sleep time between two prompts to the LLM.
