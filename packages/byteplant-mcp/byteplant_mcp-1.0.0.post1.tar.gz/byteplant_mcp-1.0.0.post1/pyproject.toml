[project]
name = "byteplant-mcp"
version = "1.0.0.post1"
description = "Byteplant's Email Validator, Phone Validator and Address Validator MCP Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.23.1",
    "requests>=2.32.5",
]
