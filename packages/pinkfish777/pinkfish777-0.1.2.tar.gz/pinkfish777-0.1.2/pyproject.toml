[project]
name = "pinkfish777"
version = "0.1.2"
description = "A backtester and spreadsheet library for security analysis."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["matplotlib>=3.10.8", "pandas>=2.3.3", "seaborn>=0.13.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
