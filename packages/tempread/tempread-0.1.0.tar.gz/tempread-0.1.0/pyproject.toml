[project]
name = "tempread"
version = "0.1.0"
authors = [
    {name = "Leon Krämer",email = "leon.kraemer.dev@gmail.com"}
]
description = "CLI-tool to display CPU-temperatures"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "psutil"
]


[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
tempread = "tempread.main:main"