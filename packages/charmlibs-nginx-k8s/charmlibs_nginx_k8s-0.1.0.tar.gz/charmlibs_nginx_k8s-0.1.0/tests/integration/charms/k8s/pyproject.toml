[project]
name = "test"
version = "0.1.0"
description = "Test charm for integration tests"
requires-python = ">=3.10"
dependencies = [
    "ops==3.*",
    "charmlibs-nginx-k8s",
]

[tool.uv.sources]
charmlibs-nginx-k8s = { path = "nginx_k8s" }
