[project]
name = "uniarray"
version = "0.0.1"
description = "Device-agnostic array library (coming soon)"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Dror"}]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

