ci:
  autoupdate_schedule: quarterly
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version
    rev: v0.12.0
    hooks:
    # run the linter
    - id: ruff
      args: [ --fix ]
    # run the formatter
    - id: ruff-format
        
