[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "python -m gade.api.server"
healthcheckPath = "/"
healthcheckTimeout = 100

[[services]]
name = "gade-api"
internalPort = 8000

[variables]
PORT = "8000"
