[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stego-client"          # choose a unique name on PyPI
version = "0.1.0"
description = "Tiny client for LLM stego encode/decode over Modal"
authors = [{ name = "Your Name" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests>=2.0"]

[project.urls]
Homepage = "https://github.com/youruser/stego-client"
