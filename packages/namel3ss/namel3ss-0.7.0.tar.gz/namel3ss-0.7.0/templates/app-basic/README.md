# Basic App Template

Contents:
- `app.ai`: minimal app/page, dummy model, ai call, and a flow.

Usage:
```
n3 parse app.ai
n3 run --file app.ai hello
```

The dummy model makes outputs deterministic for development and CI.
