<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>
        {{ app_config['web_page']['title']|e }}
    </title>
    {% include "_head_meta.html.j2" %}
    <script type="module" src="/static/clipboard.js"></script>
</head>

<body>
    <main class="contained-content">
        <h1>{{ app_config['web_page']['title']|e }}</h1>
        {{ header | safe }}
        <h3>{{ app_config['web_page']['description']|e }}</h3>
        <h3>For instructions on how to add a podcast url to a podcast app, <a href="/guide.html">check out the
                guide</a>.
        </h3>
        {%- for podcast in podcasts %}
        <h2>{{ podcast['new_name']|e }}</h2>
        {% if podcast['description'] %}<h3>{{ podcast['description']|e }}</h3>{% endif -%}
        <input type="text" readonly value="{{app_config['inet_path']}}rss/{{ podcast['name_one_word']}}"
            id="{{ podcast['name_one_word']}}">
        <button type="button" class="button" onclick="window.grabToClipboard('{{ podcast['name_one_word']}}')"
            id="{{ podcast['name_one_word']}}_button">Copy url</button>{% endfor %}
        <footer>
            <hr>
            Contact: <a href="mailto:{{ app_config['web_page']['contact']|e }}">{{
                app_config['web_page']['contact']|e }}</a>
        </footer>
    </main>
</body>

</html>
