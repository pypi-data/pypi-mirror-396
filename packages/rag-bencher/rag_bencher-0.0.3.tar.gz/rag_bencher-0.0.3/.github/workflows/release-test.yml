name: Build & Release Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

env:
  IS_CHANGELOG_PR: ${{ github.event_name == 'pull_request' && startsWith(github.head_ref, 'chore/changelog-release-') }}

jobs:
  build-test-package:
    name: Build Test Package
    uses: ./.github/workflows/_build.yml
    with:
      python-version: "3.12"

  conda-build-test:
    name: Conda Build Test
    needs: build-test-package
    runs-on: ubuntu-latest

    steps:
      - name: Skip conda build for changelog PR
        if: ${{ fromJSON(env.IS_CHANGELOG_PR) }}
        run: echo "Changelog-only PR - skipping conda build."

      - name: Checkout
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        uses: actions/checkout@v4

      - name: Download built distributions
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Set up micromamba
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        uses: mamba-org/setup-micromamba@v2
        with:
          environment-name: conda-build-env
          create-args: >-
            python=3.12
            conda-build
            -c conda-forge

      - name: Determine PKG_VERSION from sdist
        id: version
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        shell: bash -l {0}
        run: |
          python - <<'PY'
          import pathlib, re, os

          dist = pathlib.Path("dist")
          candidates = sorted(dist.glob("rag_bencher-*.tar.gz"))
          if not candidates:
              raise SystemExit("No rag_bencher sdist found in dist/")

          sdist = candidates[-1]
          m = re.match(r"rag_bencher-(.+)\.tar\.gz", sdist.name)
          if not m:
              raise SystemExit(f"Unexpected sdist name: {sdist.name}")

          version = m.group(1)
          print("Using PKG_VERSION:", version)

          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              f.write(f"PKG_VERSION={version}\n")
          PY

      - name: Build and test conda package
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        shell: bash -l {0}
        env:
          PKG_VERSION: ${{ steps.version.outputs.PKG_VERSION }}
        run: |
          echo "PKG_VERSION: $PKG_VERSION"
          echo "PKG_VERSION: \"${PKG_VERSION}\"" > recipe/_pkg_version_config.yaml
          ls -la dist/
          micromamba activate conda-build-env
          conda build recipe \
            -m recipe/_pkg_version_config.yaml \
            -c conda-forge \
            --override-channels \
            --no-anaconda-upload

  publish-to-testpypi-and-smoke-test:
    name: Publish to TestPyPI And Smoke Test
    needs: conda-build-test
    runs-on: ubuntu-latest
    environment:
        name: testpypi
    permissions:
      contents: read
      id-token: write  # for Trusted Publishing

    steps:
      - name: No-op deploy for changelog PR
        if: ${{ fromJSON(env.IS_CHANGELOG_PR) }}
        run: echo "Changelog-only PR - skipping TestPyPI upload and smoke tests, but marking deployment as successful."

      - name: Download built distributions
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Upload package to TestPyPI
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

      - name: Set up Python for smoke test
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Upgrade pip
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        run: |
          python -m pip install --upgrade pip

        # Retries are needed since the upload to Test PyPI might not have been indexed yet after
        # the upload in the previous step.
      - name: Install rag-bencher for testing
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        uses: nick-fields/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 60
          retry_on: error
          shell: bash
          command: python -m pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple rag-bencher

      - name: Smoke test CLI entrypoints (pip env)
        if: ${{ !fromJSON(env.IS_CHANGELOG_PR) }}
        run: |
          python -c "import rag_bencher; print('rag_bencher OK')"
          python -m rag_bencher.cli --help
          python -m rag_bencher.bench_cli --help
          python -m rag_bencher.bench_many_cli --help
