---
include:
  - project: 'tango-controls/gitlab-ci-templates'
    file: 'PythonPublic.gitlab-ci.yml'

variables:
  TRUSTED_PUBLISHER_ENABLED: "true"

default:
  tags:
    - saas-linux-small-amd64

# Disable tests until we fix the mocker issue
run-pytest:
  rules:
    - when: never

run-pytest-with-tangodb:
  rules:
    - when: never