Position = tuple[int, int]
"""Grid position as (x, y) coordinate where x is column and y is row."""

ViewportSize = tuple[int, int]
"""Viewport size as (columns, rows) to display centered around a position."""
