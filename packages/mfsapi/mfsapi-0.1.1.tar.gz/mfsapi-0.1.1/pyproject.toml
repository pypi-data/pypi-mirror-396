[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mfsapi"
version = "0.1.1"
description = "SSAPI — Fully free and extensible server/client skeleton"
authors = [{ name = "maksalmaz" }]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }