[project]
name = "first-package-manager"
version = "0.1.0"
description = "the four basic operations as a package"
readme = "README.md"
authors = [
    { name = "james", email = "james.paul@zohocorp.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
first-package-manager = "first_package_manager:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
