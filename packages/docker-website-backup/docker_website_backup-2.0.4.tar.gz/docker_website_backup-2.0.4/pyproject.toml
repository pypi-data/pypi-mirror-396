[project]
name = "docker-website-backup"
version = "2.0.4"
requires-python = ">=3.8"
authors = [
  {name = "Bertrand Bordage", email = "bertrand@noripyt.com"},
]

[project.urls]
Homepage = "https://github.com/noripyt/docker-website-backup"

[project.scripts]
docker-website-backup = "docker_website_backup:main"
dwb = "docker_website_backup:main"

[tool.setuptools]
py-modules = ["docker_website_backup"]

[tool.ruff.format]
quote-style = "single"
