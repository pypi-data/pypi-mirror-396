[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "COOLBOOY"
version = "1.2.1"
description = "COOLBOOY-Multi-Provider AI Assistant for developers and power users"
authors = [
  {name = "𝐈𝐌 𝐂𝐎𝐎𝐋 𝐁𝐎𝐎𝐘 𝓢𝓱𝓪𝓭𝓸𝔀 𝓚𝓲𝓷𝓰", email = "coolbooy@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
keywords = ["COOLBOOY-Multi-Provider AI Assistant"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Intended Audience :: Developers"
]
dependencies = []

[project.scripts]
COOLBOOY = "COOLBOOY.cli.main:main"

[tool.setuptools]
packages = ["COOLBOOY", "COOLBOOY.cli", "COOLBOOY.core", "COOLBOOY.modes", "COOLBOOY.utils", "COOLBOOY.assets"]
include-package-data = true

[tool.setuptools.package-data]
COOLBOOY = ["assets/*"]
