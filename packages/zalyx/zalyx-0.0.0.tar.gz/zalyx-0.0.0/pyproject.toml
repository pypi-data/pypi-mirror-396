[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "zalyx"
version = "0.0.0"
description = "Placeholder package for the Zalyx project"
readme = "README.md"
requires-python = ">=3.9"
