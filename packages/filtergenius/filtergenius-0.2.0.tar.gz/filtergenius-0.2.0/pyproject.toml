[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "filtergenius"
version = "0.2.0"
description = "FilterGenius: Smart filters to detect email, phone, emoji, numeric types, and strings."
readme = "README.md"
requires-python = ">=3.7"
authors = [
  {name="sanjeev", email="sanjeevsanju929@gmail.com"}
]
license = {text = "MIT"}
keywords = ["filter", "email", "phone", "emoji", "string", "number"]

[project.urls]
Homepage = "https://github.com/sanjeev929/filtergenius"
