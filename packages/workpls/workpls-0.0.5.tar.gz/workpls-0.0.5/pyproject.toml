[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "workpls"
description = "it will work for sure this time"
version = "0.0.5"

[project.scripts]
sample = "scr.__main__:main"