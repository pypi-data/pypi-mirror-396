[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "workpls"
description = "A sample Python project"
version = "0.0.1"

[project.scripts]
sample = "__main__:main"