[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "workpls"
description = "A sample Python project"
version = "0.0.3"

[project.scripts]
sample = "scr.__main__:main"