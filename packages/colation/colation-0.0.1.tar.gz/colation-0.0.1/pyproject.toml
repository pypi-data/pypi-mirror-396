[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "colation"
version = "0.0.1"
description = ":Colation CLI text-engine – namespace placeholder package"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
  { name = "fix", email = "fixdot@gmail.com" }
]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/colation-tools/colation"
"Source"   = "https://github.com/colation-tools/colation"
"Tracker"  = "https://github.com/colation-tools/colation/issues"
