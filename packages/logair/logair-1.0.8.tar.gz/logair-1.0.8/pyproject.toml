[project]
name = "logair"
version = "1.0.8"
description = ""
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "loguru>=0.7.3",
]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["logair", "logair.*"]