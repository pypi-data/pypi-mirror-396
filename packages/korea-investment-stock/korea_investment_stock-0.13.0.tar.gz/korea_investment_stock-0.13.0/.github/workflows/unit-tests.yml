name: Tests

on:
  pull_request:
    branches: [main, master]
    types: [opened, synchronize, reopened]

jobs:
  unit-tests:
    name: Unit Tests (Python 3.12)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: 'pip'
          cache-dependency-path: 'pyproject.toml'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run unit tests
        id: pytest
        run: |
          pytest korea_investment_stock/ -v \
            -m "not integration" \
            --ignore=korea_investment_stock/test_korea_investment_stock.py \
            --ignore=korea_investment_stock/test_integration_us_stocks.py \
            --ignore=korea_investment_stock/cache/test_cached_integration.py \
            -k "not (Redis or redis_storage or TestTokenStorageIntegration)" \
            > test-output.txt 2>&1
        continue-on-error: true

      - name: Comment test results on PR
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('test-output.txt', 'utf8');

            // Extract summary from pytest output
            const lines = output.split('\n');
            const summaryLine = lines.find(line => line.includes('passed') || line.includes('failed'));
            const lastLines = lines.slice(-15).join('\n');

            const comment = `## 🧪 Unit Test Results

            \`\`\`
            ${summaryLine || 'Test results not found'}
            \`\`\`

            <details>
            <summary>📋 Test Output (last 15 lines)</summary>

            \`\`\`
            ${lastLines}
            \`\`\`
            </details>
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

  integration-tests:
    name: Integration Tests (Python 3.12 + Docker)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: 'pip'
          cache-dependency-path: 'pyproject.toml'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,redis]"

      - name: Run integration tests
        id: integration-pytest
        run: |
          pytest -m integration -v > integration-test-output.txt 2>&1
        continue-on-error: true

      - name: Comment integration test results on PR
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('integration-test-output.txt', 'utf8');

            // Extract summary from pytest output
            const lines = output.split('\n');
            const summaryLine = lines.find(line => line.includes('passed') || line.includes('failed') || line.includes('skipped'));
            const lastLines = lines.slice(-15).join('\n');

            const comment = `## 🐳 Integration Test Results (Testcontainers)

            \`\`\`
            ${summaryLine || 'Test results not found'}
            \`\`\`

            <details>
            <summary>📋 Test Output (last 15 lines)</summary>

            \`\`\`
            ${lastLines}
            \`\`\`
            </details>
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
