"""
AnyTask Worker System

A structured task execution system inspired by GitHub Actions.
Automatically picks tasks, executes workflows, and manages task lifecycle.

Use direct imports:
    from worker.coordinator import TaskCoordinator
    from worker.executor import WorkflowExecutor
    from worker.workflow_models import Workflow, WorkflowStep
"""
