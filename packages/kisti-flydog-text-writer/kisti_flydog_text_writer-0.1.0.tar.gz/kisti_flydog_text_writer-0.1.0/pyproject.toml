[project]
name = "kisti-flydog-text-writer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "build>=1.3.0",
    "mcp[cli]>=1.24.0",
    "twine>=6.2.0",
]

[project.scripts]
kisti-flydog-text-writer = "kisti_flydog_text_writer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
