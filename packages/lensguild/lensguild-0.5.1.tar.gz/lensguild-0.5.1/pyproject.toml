[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lensguild"
version = "0.5.1"
description = "Optical design tools"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"  # SPDX short form is fine
license-files = ["LICENSE"]
authors = [{ name = "Anton Sharapov", email = "sharapov.anton@gmail.com" }]
dependencies = [
    "numpy>=1.23",
    "allytools>=0.6.0",
    "isensor>=0.2.0"
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Operating System :: OS Independent",
  "Topic :: Software Development :: Libraries",
  "Typing :: Typed",
]

[project.optional-dependencies]
dev = ["pytest", "mypy", "ruff"]
qa  = ["tox", "pre-commit"]
gui = ["PyQt5"]

[project.urls]
Homepage = "https://github.com/SharapovAnton/lensguild"
Source   = "https://github.com/SharapovAnton/lensguild"
Issues   = "https://github.com/SharapovAnton/lensguild/issues"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["lensguild*"]
exclude = ["tests*", "examples*"]

[tool.setuptools.package-data]
lensguild = ["py.typed"]