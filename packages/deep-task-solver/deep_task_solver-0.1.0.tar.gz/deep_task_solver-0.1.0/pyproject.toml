[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deep-task-solver"
version = "0.1.0"
description = "Простая pip-библиотека для отправки задач в DeepSeek"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

authors = [
  { name = "YOUR NAME", email = "you@example.com" }
]

dependencies = [
  "requests>=2.31"
]

[project.urls]
Homepage = "https://github.com/yourname/deep-task-solver"
