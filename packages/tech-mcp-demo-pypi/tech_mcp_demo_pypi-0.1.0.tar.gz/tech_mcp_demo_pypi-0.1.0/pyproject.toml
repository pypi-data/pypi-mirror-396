[project]
name = "tech-mcp-demo-pypi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "顾苗苗", email = "gumiaomiao@mininglamp.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.23.2",
]

[project.scripts]
tech-mcp-demo-pypi = "tech_mcp_demo_pypi:main"

[build-system]
requires = ["uv_build>=0.8.3,<0.9.0"]
build-backend = "uv_build"
