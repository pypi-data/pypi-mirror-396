[project]
name = "optimflow"
version = "0.0.4"
dependencies = ["matplotlib", "pandas", "param", "pymoo", "smt", "ipykernel"]
requires-python = ">=3.9"

readme = { file = "README.md", content-type = "text/markdown" }

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
  "build",
  "twine",
]
