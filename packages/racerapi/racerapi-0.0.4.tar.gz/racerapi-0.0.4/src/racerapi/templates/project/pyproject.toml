
```toml
[project]
name = "{{ project_slug }}"
version = "0.0.3"
description = "{{ project_name }} backend service"
requires-python = ">=3.8"

dependencies = ["fastapi", "uvicorn[standard]", "python-dotenv", "RacerAPI"]
