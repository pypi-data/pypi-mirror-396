[project]
name = "doguda"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
    "typer>=0.12.0",
    "pydantic>=2.7.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[project.scripts]
doguda = "doguda.__main__:main"

[tool.uv]
package = true
