[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "unika-core"
version = "0.1.0"
description = "Core utilities for Unika"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["unika_core"]
