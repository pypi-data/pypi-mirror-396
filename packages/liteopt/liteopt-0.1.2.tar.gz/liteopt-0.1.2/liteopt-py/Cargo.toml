[package]
name = "liteopt-py"
version = "0.1.2"
edition = "2021"
readme = "README.md"

[lib]
name = "liteopt"
crate-type = ["cdylib"]

[dependencies]
liteopt_core = { package = "liteopt", path = "../liteopt-core" }
numpy = "0.27.1"
pyo3 = { version = "0.27", features = ["extension-module", "abi3"] }
