[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nx_hif"
version = "0.0.11"
description = "Higher-order networks for NetworkX using HIF"
readme = "README.md"
dependencies = [
  "networkx",
]

[project.urls]
"Source" = "https://github.com/colltoaction/nx_hif"
