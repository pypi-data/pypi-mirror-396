[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "saispec"
version = "0.1.0"
description = "Governance-first runtime for AI agent safety, permissions, and accountability"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }

authors = [
  { name = "SaiSpec Labs" }
]

dependencies = []

[project.urls]
Homepage = "https://github.com/SaiSpec-Labs/saispec"
Repository = "https://github.com/SaiSpec-Labs/saispec"

