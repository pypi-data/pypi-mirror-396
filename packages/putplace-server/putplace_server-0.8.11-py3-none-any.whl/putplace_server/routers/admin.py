"""Admin routes for PutPlace.

Note: The admin dashboard and other complex HTML pages are still in main.py
pending full template extraction. This router contains placeholders.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/admin", tags=["admin"])

# Admin dashboard and other admin pages are currently in main.py
# They will be migrated here once the HTML templates are fully extracted
