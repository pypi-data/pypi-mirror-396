[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fog2d"
version = "0.1.3"
description = "Fog2D – Console 2D Engine with pseudo-graphics, lighting and collision"
dependencies = ["colorama"]
authors = [{name="XDevster"}]
license = { text = "BSD 3-Clause License" }