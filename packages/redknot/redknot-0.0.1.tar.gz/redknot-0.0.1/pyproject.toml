[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "redknot"
version = "0.0.1"
description = "A lightweight dependency graph generator for C and more."
readme = "README.md"
authors = [{ name = "Sanyam Asthana", email = "sanyam.asthana@gmail.com" }]
dependencies = []

[project.scripts]
redknot = "redknot.main:main"
