[project]
name = "nushyax"
version = "0.1.0"
description = "Framework-agnostic developer CLI"
readme = "README.md"
license = { text = "MIT" }
authors = [
  { name = "Dhanush Ballanki" }
]
requires-python = ">=3.9"

dependencies = [
  "typer",
  "rich",
  "pyyaml"
]

[project.scripts]
nushyax = "nushyax.cli:app"
