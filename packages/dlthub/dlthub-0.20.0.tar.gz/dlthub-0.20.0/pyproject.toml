[project]
name = "dlthub"
version = "0.20.0"
description = "dlthub is a commercial extension to dlt"
requires-python = ">=3.9,<3.15"
authors = [
  { name = "dltHub Inc.", email = "services@dlthub.com" },
]
maintainers = [
  { name = "Marcin Rudolf", email = "marcin@dlthub.com" },
  { name = "Adrian Brudaru", email = "adrian@dlthub.com" },
  { name = "Anton Burnashev", email = "anton@dlthub.com" },
  { name = "David Scharf", email = "david@dlthub.com" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
    "Operating System :: Microsoft :: Windows",
  ]
readme = "README.md"
license = { file = "LICENSE.md" }
# no direct dlt dependency
dependencies = [
    "ruamel-yaml>=0.18.10", # could be dep group cli... TODO: move to OSS to replace pyyaml
    "python-jose>=3.5.0",
]
keywords = [ "etl" ]


[project.optional-dependencies]
dbt = [
    "jinja2>=3.1.2",
    "pydantic>2,<2.12",
    "dbt-core<1.10",
]
cache = [
  "duckdb>0.9",
]
mssql = [
  "sqlalchemy>1.4",
  "pyodbc>=4.0.39",
]
iceberg = [
  "pyiceberg>=0.9.1",
  "pyarrow>=17",
  "sqlalchemy>2",
  "duckdb>=1.3.2",
  "boto3>=1.37.3"
]

snowflake = [
    "dlt[snowflake]",
]
mcp = [
  "mcp>=1.2.1",
  "duckdb>=1.1.3",
  "pyarrow>=17",
  "pandas>=2.1.4",
]

# mark as OSS dlt extension
[project.entry-points.dlt]
# must belong to dlt-plugins
dlthub = "dlthub.__plugin__"

# mark as dlt plus package
# [project.entry-points.dlt_package]
# dlt-project = "dlthub"

[tool.hatch.build.targets.sdist]
packages = ["dlthub"]

[tool.hatch.build.targets.wheel]
packages = ["dlthub"]

[tool.hatch.build.targets.sdist.force-include]
"dlthub/_runner/trace.schema.yaml" = "dlthub/_runner/trace.schema.yaml"

[tool.hatch.build.targets.wheel.force-include]
"dlthub/_runner/trace.schema.yaml" = "dlthub/_runner/trace.schema.yaml"

[build-system]
requires = ["hatchling>=1.20.0"]
build-backend = "hatchling.build"
