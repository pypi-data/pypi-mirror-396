"""
Local development settings for {{ project_name }}.

This file contains settings specific to local development:
- DEBUG mode enabled
- SQLite database for quick setup (NOT for production!)
- Django Debug Toolbar (optional)
- Relaxed security settings

DO NOT use these settings in production!

⚠️  IMPORTANT: SQLite is ONLY for quick local testing.
   For production-like development, use Docker with PostgreSQL:
   $ quickscale up

   Production REQUIRES PostgreSQL - no SQLite fallback is available.
"""

from .base import *  # noqa: F403

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# Allow all hosts in development (including Docker containers running on 0.0.0.0)
ALLOWED_HOSTS = ["localhost", "127.0.0.1", "[::1]", "0.0.0.0", "*"]

# Database - SQLite for quick local development
# For local PostgreSQL, use: postgresql://user:password@localhost:5432/{{ project_name }}
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": BASE_DIR / "db.sqlite3",  # noqa: F405
    }
}

# Django Debug Toolbar (uncomment to enable)
# INSTALLED_APPS += ["debug_toolbar"]  # noqa: F405
# MIDDLEWARE += ["debug_toolbar.middleware.DebugToolbarMiddleware"]  # noqa: F405
# INTERNAL_IPS = ["127.0.0.1"]

# Email backend - console output for development
EMAIL_BACKEND = "django.core.mail.backends.console.EmailBackend"

# Disable password validation in development (optional, for convenience)
# AUTH_PASSWORD_VALIDATORS = []  # noqa: F405

# Simplified static file serving in development
STORAGES = {
    "default": {
        "BACKEND": "django.core.files.storage.FileSystemStorage",
    },
    "staticfiles": {
        "BACKEND": "django.contrib.staticfiles.storage.StaticFilesStorage",
    },
}

# CORS settings (if using Django CORS Headers)
# CORS_ALLOW_ALL_ORIGINS = True  # Only for development!

# Logging - more verbose in development
LOGGING = {
    "version": 1,
    "disable_existing_loggers": False,
    "formatters": {
        "verbose": {
            "format": "{levelname} {asctime} {module} {message}",
            "style": "{",
        },
    },
    "handlers": {
        "console": {
            "class": "logging.StreamHandler",
            "formatter": "verbose",
        },
    },
    "root": {
        "handlers": ["console"],
        "level": "DEBUG",
    },
    "loggers": {
        "django": {
            "handlers": ["console"],
            "level": "INFO",
            "propagate": False,
        },
        "{{ project_name }}": {
            "handlers": ["console"],
            "level": "DEBUG",
            "propagate": False,
        },
    },
}
